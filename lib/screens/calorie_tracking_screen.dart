import 'package:flutter/material.dart';

class CalorieTrackingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Calorie Tracking'),
      ),
    );
  }
}
